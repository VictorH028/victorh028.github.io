#!/data/data/com.termux/files/usr/bin/bash

#
# Antes de que el paquete se desinstale del sistema.
#

if [[ -d ${PREFIX}/include/fmt ]];
then 
  rm -rf ${PREFIX}/include/fmt 
fi

cd ${PREFIX}/lib
rm -f  "libfmt.so.9.1.0" 
rm -f  "libfmt.so.9" 
rm -f  "libfmt.so" 

printf "%d Bey..." ${echo $?}