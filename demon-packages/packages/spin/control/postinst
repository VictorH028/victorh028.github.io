#!/data/data/com.termux/files/usr/bin/bash
#
# Se ejecuta despues que el paquete esta en el sistema 
#
set -e 
bash $PREFIX/bin/install-spin
echo "'spin' Installs correctly"
rm -rf $PREFIX/bin/install-spin  

# Borrar clonacion 

if [ -f $HOME/spin ]; then
    rm -rf $spin_DIR
fi

exit 0
