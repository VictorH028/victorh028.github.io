#!/data/data/com.termux/files/usr/bin/bash 
#
# Se ejecuta antes que el paquete se desintale del sistema  
# 
set -e 
rm ${PREFIX}/share/man/man1/spix.1 ${PREFIX}/bin/spix 2>/dev/null 
echo "Remove spix"
exit 0
